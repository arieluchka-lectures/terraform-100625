## Ports

The application uses the following port mapping:

8080:80

- Host port: 8080
- Container port: 80

## Instance Requirements

Nextcloud requires ≥128MB RAM per process (512MB recommended); 20–30GB storage allocated; EC2 instance: t3.micro (1GB RAM).

